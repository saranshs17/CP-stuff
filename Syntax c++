#include<bits/stdc++.h>
#define sped ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL); 
#define int long long 
#define ln '\n'
#define loop(n) for(int i=0;i<n;++i)
#define sortf(arr) sort(arr.begin(),arr.end())
#define sortr(arr) sort(arr.rbegin(),arr.rend())
using namespace std;
const long long M=1e9+7;

//<---------------------------code begins--------------------------->

int32_t main(){
    sped
    
}

// test case input
    int t;
    cin>>t;
    while(t--){
    }

// binary exponentiation recursive
    long long binExpRecur(int a,int b){
    if(b==0)return 1;
    ll res=binExpRecur(a,b/2);
    if(b&1){
        return a*1LL*res*res;
    }else{
        return res*res*1LL;
    }
}

//square root using binary search
